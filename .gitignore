# Build
build/
.gradle/
out/
bin/

# Secrets — KESINLIKLE PUSH ETMEYIN
firebase-service-account.json
.env
.env.*
*.keystore
*.jks
conf/secrets/
# TLS private key + sertifikalar (Cloudflare origin key) — ASLA commit'lenmez
conf/nginx/certs/
*.key
*.pem
# docker-compose: PANEL_GATE_TOKEN vb. acik sirlar iceriyor — commit'lenmez
docker-compose.yml
docker-compose.*.yml
compose.yml
compose.*.yml
# Indirilen sunucu backup/yedek config'leri
conf/nginx/*.bak.*
conf/nginx/*.before-*
conf/nginx/*.yedek*
conf/nginx/*.live.*
conf/nginx/*.maintenance.*
# application-prod.yml: yalnizca ${ENV} placeholder icerir (gercek sirlar .env'de),
# o yuzden commit'lenir — deploy'da prod datasource/profil config'i gelsin.
application-secrets.yml
application-local.yml

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.project
.classpath
.settings/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
logs/
