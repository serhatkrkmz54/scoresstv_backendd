package com.scorestv.news;

/**
 * Haber kategorisi. STRING olarak saklanir (@Enumerated(EnumType.STRING)).
 */
public enum NewsCategory {
    TRANSFER,
    MATCH,
    INJURY,
    INTERVIEW,
    PREVIEW,
    RESULT,
    GENERAL
}
