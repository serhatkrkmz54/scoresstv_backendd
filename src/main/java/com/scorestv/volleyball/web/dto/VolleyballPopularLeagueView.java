package com.scorestv.volleyball.web.dto;

/** Web sol ray populer voleybol ligi satiri — basketbol esi. */
public record VolleyballPopularLeagueView(
        Long id,
        String name,
        String slug,
        String logo) {}
